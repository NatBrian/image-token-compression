# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
*.env

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Runtime logs / event data
*.log
.imgctx/

# Raw request/response capture (imgctx proxy IMGCTX_CAPTURE_DIR). Heavy, per-call,
# regenerable by rerunning; results*.json + REPORT.md stay tracked, this doesn't.
# Two dir shapes: per-item `capture/` and the claude harness's per-arm `capture_off/`
# `capture_on/` (IMGCTX_CAPTURE_DIR is set once per proxy, not per item).
**/capture/
**/capture_*/

# Session scratch (created by tooling, not part of the project)
.remember/

# Benchmark outputs
bench/last_result.json
bench/baseline_result.json

# Benchmark raw per-item artifacts (kept on disk for inspection, NOT committed).
# For every benchmark the summary REPORT.md + aggregated results.json ARE tracked;
# the heavy/transient run data (repo checkouts, per-item streams, patches, proxy
# event logs, run-local backups) is ignored.

# HotpotQA (OpenCode path). Wildcard covers every model-tagged run folder
# (hotpot_opencode_runs, hotpot_opencode_runs_gemini_31_flash_lite_v2, etc.) so a new
# --runs-dir never needs a new gitignore line. Track results*.json; ignore per-item.
bench/hotpot_runs/off/
bench/hotpot_runs/on/
bench/hotpot_runs/*.log
bench/hotpot_opencode_runs*/off/
bench/hotpot_opencode_runs*/on/

# HotpotQA post-refactor verification, all 4 CLIs (bench/hotpot_verify_runs/*). Track
# REPORT.md + results*.json at each run root; ignore heavy per-item off/on dirs,
# per-arm captures, agent streams, and proxy event logs.
bench/hotpot_verify_runs/**/off/
bench/hotpot_verify_runs/**/on/
bench/hotpot_verify_runs/**/*.log
bench/hotpot_verify_runs/**/proxy_*events*.jsonl

# HotpotQA (Claude Code path)
bench/hotpot_claude_runs/off/
bench/hotpot_claude_runs/on/
bench/hotpot_claude_runs/_sonnet_streams/
bench/hotpot_claude_runs/*.log
bench/hotpot_claude_runs/proxy_*events*.jsonl
bench/hotpot_claude_runs/results_*.json
# but DO track the IMGCTX_TOOLS=0 rerun's aggregate (backs the deep-dive Section 13
# "measured flip" numbers); heavy per-item streams under off_tools0/on_tools0 stay out.
!bench/hotpot_claude_runs/results_tools0.json
bench/hotpot_claude_runs/off_tools0/
bench/hotpot_claude_runs/on_tools0/

# Long-doc single-shot (Claude Code path). Track LONGDOC_REPORT.md + results_*.json
# + the cached data_*.json question sets; ignore heavy per-item streams/logs.
# (longdoc_runs is the pre-rename name, longdoc_claude_runs the current one -- keep
# both so a stale local checkout from before the rename still ignores correctly.)
bench/longdoc_runs/off/
bench/longdoc_runs/on/
bench/longdoc_runs/*.log
bench/longdoc_runs/proxy_*events*.jsonl
bench/longdoc_runs/results.json
bench/longdoc_runs/data_*.json
bench/longdoc_claude_runs/off/
bench/longdoc_claude_runs/on/
bench/longdoc_claude_runs/*.log
bench/longdoc_claude_runs/proxy_*events*.jsonl
bench/longdoc_claude_runs/results.json
bench/longdoc_claude_runs/data_*.json

# Long-doc single-shot (OpenCode path, any model-tagged --runs-dir). Track results*.json.
bench/longdoc_opencode_runs*/off/
bench/longdoc_opencode_runs*/on/
# Junk run: gemini-3.1-flash-lite went unavailable mid-run -> all-error, zero-signal
# data (no valid tokens/answers). Exclude the whole folder, results included.
bench/longdoc_opencode_runs_gemini_31_flash_lite/

# SWE-bench (OpenCode path, any model-tagged --runs-dir). Track results*.json + instances.json.
bench/swebench_opencode_runs*/_repos_cache/
bench/swebench_opencode_runs*/off/
bench/swebench_opencode_runs*/on/

# SWE-bench (Claude Code path). Same pre-/post-rename note as long-doc above.
bench/swebench_runs/_repos_cache/
bench/swebench_runs/off/
bench/swebench_runs/on/
bench/swebench_runs/*.log
bench/swebench_runs/proxy_*events*.jsonl
bench/swebench_runs/results_*.json
bench/swebench_claude_runs/_repos_cache/
bench/swebench_claude_runs/off/
bench/swebench_claude_runs/on/
bench/swebench_claude_runs/*.log
bench/swebench_claude_runs/proxy_*events*.jsonl
bench/swebench_claude_runs/results_*.json

tmp-test